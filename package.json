{
  "name": "anu-frequency-studio",
  "version": "1.0.0",
  "description": "Personal Frequency Studio - A modular frequency therapy lab",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && npm run dev",
    "client": "cd client && npm start",
    "build": "cd client && npm run build",
    "install-all": "npm install && cd server && npm install && cd ../client && npm install"
  },
  "keywords": ["frequency", "therapy", "sound", "healing", "bioresonance"],
  "author": "AnuFrequency Studio",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}